kernel_clauses:
  energy_conservation: "Total vitality cannot be created from nothing"
  identity_permanence: "Agent IDs are permanent and unique"
  determinism: "Same seed + same actions = same outcome"
  canary_before_commit: "No mechanism executes on live state without prior canary testing"
  recovery_accessibility: "Agents may propose checkpoint reversion as a mechanism"

governance_clauses:
  max_tax_rate: "No mechanism may extract more than 50% of an agent's vitality"
  proposal_limit: "One mechanism proposal per agent per round"
  voting_process: "All living agents vote; majority threshold activates; agents may amend this rule"
  activation_timing: "Mechanisms defer until vote threshold met; no artificial deadline"
  judge_role: "The judge provides advisory opinions; it does not have veto power"
  canary_divergence_threshold: "Flagged if total vitality drops >50% or any agent dies unexpectedly"

world_rules: {}
