# ─── Leviathan Environment Configuration ───────────────────────
# Copy this file to .env and fill in your values.
# Only LLM keys are required; everything else has sensible defaults.

# ─── LLM Provider (pick one) ───────────────────────────────────
# OpenRouter (recommended — access to many models via one key)
OPENROUTER_API_KEY=sk-or-...
# OPENROUTER_BASE_URL=https://openrouter.ai/api/v1  # default

# OpenAI / OpenAI-compatible gateway
# OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1

# Offline mode — skip all LLM calls (useful for testing)
# LLM_OFFLINE=1

# ─── E2E Smoke Test ────────────────────────────────────────────
# E2E_PROVIDER=openrouter        # or: openai
# E2E_MODEL=minimax/minimax-m2.1 # optional model override

# ─── Server Configuration ──────────────────────────────────────
# These map to run_server.py CLI args and docker-compose env vars.
LEVIATHAN_MEMBERS=10        # Number of island members (agents)
LEVIATHAN_LAND=20x20        # Land grid dimensions (WxH)
LEVIATHAN_SEED=42           # Random seed for deterministic replay
LEVIATHAN_PORT=8000         # HTTP port
LEVIATHAN_PACE=2.0          # Seconds per submission window
LEVIATHAN_ROUNDS=0          # Max rounds (0 = unlimited)
LEVIATHAN_RATE_LIMIT=60     # Max requests/min per client IP

# ─── Authentication (optional) ─────────────────────────────────
# Comma-separated API keys. Empty = open access (good for local dev).
# LEVIATHAN_API_KEYS=key1,key2,key3

# Comma-separated moderator keys for admin endpoints (pause/ban/rollback).
# LEVIATHAN_MODERATOR_KEYS=mod-secret-1

# ─── Quick Profiles ────────────────────────────────────────────
#
# Local dev (fast rounds, open access):
#   LEVIATHAN_PACE=1.0
#   LEVIATHAN_MEMBERS=5
#   LLM_OFFLINE=1
#
# Production (auth enabled, longer rounds):
#   LEVIATHAN_PACE=10.0
#   LEVIATHAN_MEMBERS=20
#   LEVIATHAN_API_KEYS=your-secret-key
#   LEVIATHAN_MODERATOR_KEYS=mod-secret
